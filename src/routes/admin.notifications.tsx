import { createFileRoute } from "@tanstack/react-router";
import { SiteShell, PageIntro, Panel } from "@/components/eoz/SiteShell";
import { ADMIN_NAV, DashNav, StatTile } from "@/components/eoz/DashNav";

export const Route = createFileRoute("/admin/notifications")({ head: () => ({ meta: [{ title: "Notification Queue — EOZ Staff" }] }), component: Notifications });
function Notifications() { return <SiteShell><PageIntro eyebrow="( 06.10 ) — Notifications" title="Reliable messages, visible failures." lead="Monitor delivery, retries and unsubscribes across email and in-app alerts. Optional SMS and WhatsApp adapters stay disabled until configured." /><DashNav items={ADMIN_NAV} /><div className="grid gap-3 pb-6 sm:grid-cols-2 lg:grid-cols-4"><StatTile label="Delivered today" value="1,284" /><StatTile label="Queued" value="32" /><StatTile label="Retrying" value="7" tone="text-amber" /><StatTile label="Failed" value="2" tone="text-rose" /></div><Panel className="mb-14"><div className="label-mono">Delivery health</div><div className="mt-5 grid gap-4 sm:grid-cols-3">{[['Email','98.9%','1,102 sent'],['In-app','100%','182 sent'],['SMS adapter','Disabled','Not configured']].map(([channel, rate, count]) => <div key={channel} className="rounded-lg bg-surface-2 p-4 ring-1 ring-line"><div className="text-sm">{channel}</div><div className="mt-2 font-display text-2xl">{rate}</div><div className="mt-1 text-xs text-muted">{count}</div></div>)}</div></Panel></SiteShell>; }

import { createFileRoute } from "@tanstack/react-router";
import { SiteShell, PageIntro, Panel, Chip } from "@/components/eoz/SiteShell";
import { ADMIN_NAV, DashNav, StatTile } from "@/components/eoz/DashNav";

export const Route = createFileRoute("/admin/services")({ head: () => ({ meta: [{ title: "Service Operations — EOZ Staff" }] }), component: Services });
function Services() { return <SiteShell><PageIntro eyebrow="( 06.8 ) — Services" title="Keep every deliverable moving." lead="Assign work, watch due dates and keep quotes, revisions and customer review in one operational view." /><DashNav items={ADMIN_NAV} /><div className="grid gap-3 pb-6 sm:grid-cols-2 lg:grid-cols-4"><StatTile label="Open orders" value="18" /><StatTile label="Payment pending" value="4" tone="text-amber" /><StatTile label="Due this week" value="7" /><StatTile label="Avg. satisfaction" value="4.8/5" /></div><section className="grid gap-4 pb-14 md:grid-cols-2">{[['SVC-2026-044','ATS-friendly CV writing','Chanda Mwansa','In progress','Due today'],['SVC-2026-043','LinkedIn optimisation','BrightPath NGO','Customer review','Due 08 Sep'],['SVC-2026-041','Job advertising','Mfumu Analytics','Payment pending','Awaiting payment'],['SVC-2026-038','Interview coaching','Luka Tembo','Assigned','Due 11 Sep']].map(([ref, service, customer, status, due]) => <Panel key={ref}><div className="flex justify-between gap-3"><span className="font-mono text-xs text-accent-soft">{ref}</span><Chip tone={status === 'Payment pending' ? 'amber' : status === 'Customer review' ? 'emerald' : 'muted'}>{status}</Chip></div><h2 className="mt-3 font-display text-xl">{service}</h2><div className="mt-1 text-sm text-muted">{customer}</div><div className="mt-5 flex justify-between border-t border-line pt-3 text-xs text-muted"><span>Assigned: Service desk</span><span>{due}</span></div></Panel>)}</section></SiteShell>; }

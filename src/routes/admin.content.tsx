import { createFileRoute } from "@tanstack/react-router";
import { SiteShell, PageIntro, Panel, Chip } from "@/components/eoz/SiteShell";
import { ADMIN_NAV, DashNav } from "@/components/eoz/DashNav";

export const Route = createFileRoute("/admin/content")({ head: () => ({ meta: [{ title: "Content Calendar — EOZ Staff" }] }), component: Content });
function Content() { return <SiteShell><PageIntro eyebrow="( 06.9 ) — Content" title="One verified fact set, many channels." lead="Create channel variants from canonical opportunities without changing deadlines, eligibility, location or official application routes." /><DashNav items={ADMIN_NAV} /><section className="grid gap-6 pb-14 lg:grid-cols-12"><Panel className="lg:col-span-8"><div className="flex items-center justify-between"><div className="label-mono">This week</div><button className="accent-gradient rounded-md px-3 py-2 text-xs font-medium text-ink">New content item</button></div><div className="mt-4 space-y-3">{[['Today · 08:00','Morning opportunity roundup','WhatsApp · Facebook','Approved'],['Today · 14:00','Career tip: writing measurable CV achievements','LinkedIn · TikTok','Scheduled'],['Tomorrow · 18:30','Graduate opportunities reminder','WhatsApp channel','Draft'],['Thu · 12:00','Training and skills spotlight','Facebook · LinkedIn','Needs review']].map(([when, title, channels, state]) => <div key={title} className="flex flex-wrap items-center justify-between gap-4 border-t border-line py-4 first:border-0 first:pt-0"><div><div className="font-mono text-xs text-accent-soft">{when}</div><div className="mt-1 text-sm">{title}</div><div className="text-xs text-muted">{channels}</div></div><Chip tone={state === 'Approved' ? 'emerald' : state === 'Needs review' ? 'amber' : 'muted'}>{state}</Chip></div>)}</div></Panel><Panel className="lg:col-span-4"><div className="label-mono">Canonical safeguards</div><ul className="mt-4 space-y-3 text-sm text-muted"><li>Version hash: EOZ-9F3A</li><li>Official routes preserved</li><li>Facebook direct-link policy enabled</li><li>External posting requires approval</li></ul></Panel></section></SiteShell>; }

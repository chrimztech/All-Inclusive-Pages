import { createFileRoute, Link } from "@tanstack/react-router";
import { SiteShell, PageIntro, Panel, Chip } from "@/components/eoz/SiteShell";

export const Route = createFileRoute("/scam-warning")({ head: () => ({ meta: [{ title: "Scam & Fraud Warning — EOZ" }] }), component: ScamWarning });
function ScamWarning() { return <SiteShell><PageIntro eyebrow="Trust & safety" title="Pause before you pay or share documents." lead="EOZ distributes opportunities, but cannot guarantee a third party's recruitment decision. Use the official employer route shown on each listing." aside={<Panel><Chip tone="amber">Read before applying</Chip><p className="mt-3 text-sm text-muted">Never send money to secure a job, interview or scholarship.</p></Panel>} /><section className="grid gap-4 pb-8 md:grid-cols-2">{[['Check the source','Open the employer website yourself. Confirm the vacancy, deadline and contact details match the official listing.'],['Protect your documents','Only share the minimum information requested through a verified application channel. Never share passwords or one-time codes.'],['Question urgent payments','Legitimate employers do not ask candidates to pay for interviews, job offers or guaranteed selection.'],['Report suspicious listings','Send EOZ the listing reference and evidence. We can review, label or remove distribution while the employer remains responsible for its process.']].map(([title, text], i) => <Panel key={title}><div className="label-mono">0{i + 1}</div><h2 className="mt-2 font-display text-2xl">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></Panel>)}</section><Panel className="mb-14"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="label-mono">Need help?</div><p className="mt-1 text-sm text-muted">Contact EOZ support for distribution questions, but use the employer's route for applications.</p></div><Link to="/report" className="rounded-md px-4 py-2 text-sm ring-1 ring-line hover:text-accent-soft">Report a listing</Link></div></Panel></SiteShell>; }

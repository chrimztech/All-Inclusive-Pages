import { createFileRoute, Link } from "@tanstack/react-router";
import { SiteShell, PageIntro, Panel, Chip } from "@/components/eoz/SiteShell";
import { DashNav, EMPLOYER_NAV } from "@/components/eoz/DashNav";

export const Route = createFileRoute("/employers/services")({ head: () => ({ meta: [{ title: "Employer Services — EOZ" }] }), component: EmployerServices });
function EmployerServices() { return <SiteShell><PageIntro eyebrow="( 05.6 ) — Services" title="Bring EOZ closer to the work." lead="Request advertising, recruitment, staffing, training and business support without losing sight of who owns the candidate relationship." /><DashNav items={EMPLOYER_NAV} /><section className="grid gap-4 pb-14 md:grid-cols-2">{[['Job advertising','Targeted distribution for a vacancy or programme.','From K 450'],['Recruitment & staffing','A managed shortlist and structured hiring support.','Custom quote'],['Team training','Practical workshops designed around your organisation.','From K 1,500'],['Business promotion','Reach relevant audiences across EOZ channels.','From K 600']].map(([name, description, price]) => <Panel key={name}><Chip tone="accent">Service</Chip><h2 className="mt-3 font-display text-2xl">{name}</h2><p className="mt-2 text-sm text-muted">{description}</p><div className="mt-5 flex items-center justify-between"><span className="font-mono text-xs text-accent-soft">{price}</span><Link to="/contact" className="text-sm text-fg hover:text-accent-soft">Request quote →</Link></div></Panel>)}</section></SiteShell>; }
